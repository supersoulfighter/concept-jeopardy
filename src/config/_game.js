// Game-level config: canvas size, scene keys, and the asset manifest.
CJ.GAME_WIDTH = 1352;
CJ.GAME_HEIGHT = 878;
CJ.GAME_HTML_ELEMENT = 'game-container';

// Scene keys — the strings Phaser uses to start and switch scenes.
CJ.SCENES = {
	BOOT: 'Boot',
	PRELOADER: 'Preloader',
	MENU: 'MainMenu',
	BOARD: 'GameBoard',
	CLUE: 'Clue',
};
